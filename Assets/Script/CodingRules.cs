using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CodingRules : MonoBehaviour
{

    /*
        変数・関数名付けルール

        ①int,bool,Image 等   
            単語１つの場合は頭文字は大文字
            複数の単語が連なっている場合は最初の単語のみ小文字で
            その後の単語の頭文字は大文字

        ②void
            単語の頭文字は大文字(単語の数問わず)

        ③IEnumerator
            最初の単語のみ小文字
            その後の単語の頭文字は大文字

        ④スクリプトを参照させる際の名前
          そのクラス名の最初の単語のみ小文字

        ➄プロパティで参照させる変数の名前
          最初から単語の頭文字は全て大文字

        ⑥定数,const
        頭文字は全て大文字
     
     
     */
    //後で変数まとめる！！
    
    
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
